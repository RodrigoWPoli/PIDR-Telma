# Core inference and data
owlready2>=0.46
pymongo>=4.6
opcua>=0.98.13

# Dashboard
streamlit>=1.33
plotly>=5.18

# Desktop window (pywebview uses Edge WebView2 on Windows; needs pywin32 on Windows)
pywebview>=5.0
pywin32>=306; sys_platform == "win32"
